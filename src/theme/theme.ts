import { createTheme } from "@mui/material/styles";

export const themeApp = createTheme({
    palette: {
        primary: {
            main: "#5b7bb2",
            dark: "#e3f2fd",
            light: "#9bc0ff",
            contrastText: "black",
        },
    },
});
